import { PublicKey, VersionedTransaction } from "@solana/web3.js";
export declare function batchAccountsClose({ blockhash, feePayer, multisigPda, member, rentCollector, batchIndex, programId, }: {
    blockhash: string;
    feePayer: PublicKey;
    multisigPda: PublicKey;
    member: PublicKey;
    rentCollector: PublicKey;
    batchIndex: bigint;
    programId?: PublicKey;
}): VersionedTransaction;
