import { getProposalPda } from "../pda";
import { createProposalApproveInstruction } from "../generated";
export function proposalApprove({ multisigPda, transactionIndex, member, memo, programId, }) {
    const [proposalPda] = getProposalPda({
        multisigPda,
        transactionIndex,
        programId,
    });
    return createProposalApproveInstruction({ multisig: multisigPda, proposal: proposalPda, member }, { args: { memo: memo ?? null } }, programId);
}
