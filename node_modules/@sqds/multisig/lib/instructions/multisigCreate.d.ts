import { PublicKey, TransactionInstruction } from "@solana/web3.js";
import { Member } from "../generated";
/** @deprecated This instruction is deprecated and will be removed soon. Please use `multisigCreateV2` to ensure future compatibility. */
export declare function multisigCreate({ creator, multisigPda, configAuthority, threshold, members, timeLock, createKey, rentCollector, memo, programId, }: {
    creator: PublicKey;
    multisigPda: PublicKey;
    configAuthority: PublicKey | null;
    threshold: number;
    members: Member[];
    timeLock: number;
    createKey: PublicKey;
    rentCollector: PublicKey | null;
    memo?: string;
    programId?: PublicKey;
}): TransactionInstruction;
